# 0.5 follow-up audit and execution record

Date: 2026-09-15. Authorized scope: inspect and strengthen the existing 0.5 candidate, retain all 27 workstreams, use independent workers, push this scope to the existing draft PR. Merge, npm publication, deployment and paid services are excluded by the current instruction.

## Verified starting point

GitHub PR [#32](https://github.com/montassarkhalloufi/DevMethod/pull/32) was open and draft, with no discussion/review comments, head `8c23c0e921c12ebfade2ab7efe6a8e9734dc7309`, tree `16694950a2d1a8a7155a2fe24e7f7cc04446bf5e`. Current main was `03e6b1c64becf8446c5896cd2236eb3f8ad37b6d`. Both workflow runs [platforms 35002593432](https://github.com/montassarkhalloufi/DevMethod/actions/runs/35002593432) and [fullstack 35002593437](https://github.com/montassarkhalloufi/DevMethod/actions/runs/35002593437) succeeded; the three platform job conclusions were read individually. These results belong to the starting commit.

The previously tested local tree was identical. A separate clone was then based on the actual remote commit, and two separate worktrees owned the controls and evaluation changes. Existing checkouts and their edits were preserved. Integration is sequential; shared lockfiles have a single integration owner; the later quality follow-up adds pinned development tools without changing runtime dependencies.

Current runtime: Linux, Node v24.19.0, npm 11.9.0. `npm ci --offline --ignore-scripts --no-audit --no-fund` succeeded from the existing cache; baseline `npm test` actually passed **168/168**, no skips. `npm view devmethod-ai version dist-tags --json --fetch-retries=0 --fetch-timeout=15000` returned stable **0.4.1**, `latest: 0.4.1`, `next: 0.1.0-rc.2`. Thus this audit did not observe 0.5.0 published.

`command -v codex claude cursor cursor-agent gh` found none. No native model invocation or authentication attempt was made. Work Mode research/review workers are not native CLI experiments. The six-invocation historical pilot and separate app-server adapter records remain historical, scoped evidence; neither is transferred to this candidate.

## Prioritized actions and actual scope

The action boundaries were assigned before implementation. Source research used two bounded passes; the second pass was restricted to a stated problem, expected decision benefit and validation cost. See [industry sources](HARDENING-0.5-SOURCES.md) and [scientific analysis](HARDENING-0.5-SCIENCE.md). No new workstream was necessary.

| ID / priority / type | Demonstrated reason and bounded change | Owner / dependencies | Failure-detecting verification / stop condition |
|---|---|---|---|
| H01 / P2 / correction | `inspectLoop` lost known threshold crossings after a null measurement. Retain a known lower bound while exact totals remain null; reject unsafe sums. `src/loop.ts`, generated `dist/loop.js`, existing tests/docs. | controls writer; independent science reviewer; starting tree | Same frozen 155-trace BigInt oracle; null/threshold/duration/overflow regressions. Stop after preserved diagnostics with no new eligible unknown histories. No runtime executor. |
| H02 / P2 / correction | `reserveRun` admitted a subsequent run after malformed usage `{}` produced NaN. Validate recorded usage and totals; retain interrupted/unmetered history and compatible adapter terminal states. `scripts/native-host.mjs`, existing native tests/docs. | controls writer; independent science reviewer; inspect all `recordRun` callers | Missing/negative/fractional/string/unsafe counters and overflow must block without creating a slot or rewriting history. Genuine zero remains valid. No hard-token-cap claim. |
| H03 / P2 / evaluation correction | Six negative skill oracles omitted useful task completion; a no-op could pass their stated criteria. Add task-specific `.3` criteria without altering agent fixtures. `oracle.json`, scorer tests, pending score, README. | evaluation writer; guidance reviewer; fixed prompts | Omitting the new criterion yields unresolved, the actual AI-NEG subtraction bug fails its arithmetic check. Preserve 36 planned slots; no synthetic record enters native results. |
| H04 / P2 / evaluation correction | Identical transcript content/path could be reused in two native-shaped repetitions. Reject obvious reuse across invoked native runs. | evaluation writer; guidance reviewer | Same content under copied paths and same path fail validation; distinct transcripts with local label `session-1` remain accepted. This detects duplication, not genuine independence or authenticity. |
| H05 / P2 / correction | Finite metrics could aggregate to Infinity and serialize to null; unsafe integer counts lost precision. Reject invalid counts and aggregate overflow. | evaluation writer; guidance reviewer | Explicit null/zero/partial usage retained; unsafe sums and non-finite totals rejected. Do not invent campaign cost. |
| H06 / P3 / correction | Non-ISO numeric timestamps passed `Date.parse` coercion despite the report contract. Require timezone-bearing timestamps and valid calendar dates. | evaluation writer; guidance reviewer | Numeric/date-only/rollover/reversed timestamps rejected; valid offsets accepted. This is chronology validation, not a session-authenticity proof. |
| H07 / P3 / documentation correction | Unverified native menu claim and inconsistent comment attribution. Qualify installed adapters; verify source dates/bylines and comments; keep inaccessible attributions unknown. | root writer; industry researcher and guidance review | Compare changed claims with current capability evidence and primary sources; local links/skill validation. No new behavioral rule or compulsory review. |
| H08 / P2 / validation gap | Separate recorded coverage from business truth and test the scientific proposal against a simpler baseline. Retain immutable bounded probes and results. | science worker and root; baseline before H01 | 155-trace baseline/candidate comparison plus actual weak-test/contract-oracle probe with defective and healthy arithmetic implementations. No native or causal productivity inference. |
| H09 / required gate / delivery | A changed payload requires an exact archive, current CI and a reviewable handoff. | root; all integrated corrections and reviews | Full local gates, committed build, actual package inspection/smoke, current-head CI and regenerated archive hashes. Stop at draft PR/candidate; maintainer review/publication remain separate. |

## Experiments and interpretation

- **Partial observation:** fixed enumeration of 155 token histories, 71 with unknown usage. Baseline missed final threshold diagnostics in 49 traces and post-threshold diagnostics in 20; these categories overlap. The independently reviewed correction reduced both to zero, with zero unknown histories incorrectly eligible before and after. The immutable script and results are under [hardening-audit](../evaluation/hardening-audit/README.md). This is a finite diagnostic comparison, not general convergence, stability, or native model performance.
- **Evidence relevance:** the same actually executed weak assertion passes a seeded arithmetic defect and a healthy control. `closure` reports `supported` for both honestly pinned records. An external contract assertion fails on the defect and passes on the healthy control. This demonstrates the documented semantic boundary; changing `closure` to guess truth would add an unsupported contract.
- **Evaluation no-op and duplicate records:** independent audit constructed records explicitly labeled as synthetic/calibration material. Before correction, unchanged failing AI-NEG could satisfy its two declared negative criteria; a duplicated native-shaped transcript could count twice. These are defects in specification/validation, not two observed native model runs.
- **Historical review:** the retained Work Mode concurrent reservation exercise is one real reviewer observation. Its deliberately defective fixture stays unchanged. No new reviewer accuracy rate or healthy-control model campaign is claimed; the arithmetic control above is deterministic, not a model reviewer.

## What remains unexecuted and how to resume

The actual [native score](../evaluation/behavioral/pending-score.json) keeps **36 not-run slots, zero native completions, null completed-run pass rate**. The six new negative criteria change the evaluator oracle, not the fixture prompts. Preserve its digest, scorer version and fixture revision before any future run; an older record lacking a new criterion is unresolved, not a retroactive pass.

1. On a machine with an authorized authenticated host, inspect `command -v codex`, `codex --version` and `codex exec --help` (or the selected host's real executable/help). Record exact host/model/version/permissions. Do not use the macOS historical pilot as a general launcher: it has pinned fixtures/settings and a 12-invocation budget, not an 18-case campaign driver.
2. From the candidate checkout run `node scripts/prepare-behavior-case.mjs CLOSURE /absolute/fresh-case-directory > /absolute/record-outside-worker.json`. The destination must be absent with an existing parent. Then install this candidate with `node dist/cli.js init --tool codex --dest /absolute/fresh-case-directory` and verify `node dist/cli.js doctor --dest /absolute/fresh-case-directory --json` (select `claude` or `cursor` when that is the actual host). Preparation alone copies no skills. Pin the installed files and `kit-manifest.json`, source revision and archive identity before the initial snapshot and launch; a candidate arm must actually consume that installation. A deliberately no-method comparison arm must be separately declared. Repeat with each actual `caseId` in `evaluation/behavioral/cases.json`, each of its two repetitions and a fresh directory. Keep preparation records, oracle, evaluation metadata and this audit outside agent inputs. Follow each record's operator steps; preparation does not install React/PostgreSQL or launch a host.
3. Before invocation, record one fixed host/model/method/fixture configuration, 36 planned slots (two per case), sequential dispatch and a reviewed per-run duration bound. A proposed initial timeout is 120 seconds plus 1.5 seconds grace, with interrupted trials retained; validate that bound against each prepared task before freezing the campaign. Set the campaign's token/cost policy using controls actually exposed by that host. If strict caps are required but unavailable, retain the blocker. Do not reset an already consumed ledger or reuse the historical pilot's authorization exception.
4. Use the selected host's inspected command, capture its raw transcript and actual checks, and preserve original claims before independent adjudication. For RESUME preserve both actual launches and the intervening contract change. For REVIEW retain healthy/defective calibration and false positives. Unavailable setup remains not-run/blocked; an invoked interruption remains interrupted.
5. Run `node scripts/evaluate-behavior.mjs /absolute/report.json /absolute/evidence-directory`. Exit 0 means a valid score, not release success. The success criterion is auditable observations meeting the frozen oracle, with failures/missingness retained and no ungrounded native or comparative claim.

Current platform CI must be read for the final PR head, and the maintainer must review that exact diff/archive under CONTRIBUTING. No merge, registry publish or deployment command is executed in this mission. If 0.5.0 becomes published independently before resumption, recheck registry integrity and use a new version instead of attempting to overwrite it.

## Verification before the implementation-quality extension

Integrated local gates passed: `npm test` **178/178**, `npm run test:greenfield` **24/24**, documentation links, diff whitespace and committed `dist/` checks. The regenerated pending score equals the committed 36-slot record, the integrated loop probe equals the independently reviewed result, and the semantic probe reproduces both expected outcomes. The modified skill passes its structural validator. Exact archive identity and current-head CI are recorded in the draft PR after packaging. Historical counts above remain attached to their starting revision. The main [27-workstream ledger](HARDENING-0.5.md) is the reconciled capability map; this record owns this follow-up's actions and blockers.

## Implementation-quality extension

The user extended the scope to exemplary code quality, relevant tests, large-workload guidance and human responsibility. The [quality follow-up](HARDENING-0.5-QUALITY.md) owns these additions, analyzer versions, refactor evidence and remaining limits. Counts in the preceding section belong to the earlier candidate. Final package identity, integrated counts and current-head CI belong to the final draft PR handoff. No merge, publication or deployment is authorized here.
