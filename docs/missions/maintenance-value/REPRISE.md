# Maintenance checkpoint — 2026-09-16

Start with [scope](PLAN.md), [observations and decisions](RESULTS.md), [review](REVIEW.md) and actual Git/PR state. This is a dated snapshot, not renewed execution authority.

The branch `codex/maintenance-resumption-value` is based on PR #34 `5448fe82b001c592f44856baa2752c4fe4516588`. The draft delivery targets `codex/journey-evidence-evolution`, so its diff contains this tranche only. The PR owns its current head, exact archive digest and remote CI; no merge, npm publication or version bump is authorized here. Earlier PR #33/#34 evidence remains unchanged.

## Stop and remaining objective

The requested relative benefit during resumption, changed requirements and localized maintenance remains **indeterminate**. All three cases and their independent controls were prepared, but one tiny native admission failed before a JSONL event. No A/B or candidate C was executed. No new method rule, stage or generalized advantage follows.

The original root `/private/tmp/devmethod-maintenance-campaign-20260916` is stopped and must remain unchanged. Its original permit is `2733e70e5316f0ce9f10e6495b984dcb6052860e51e320bc4a61106b05296f88`. Preserve the failed admission, null usage and six not-run slots. Do not retry that slot or create a fresh directory merely to bypass the stop. The historical Repair Café campaign remains separate, with its own unresolved usage.

Useful local instrument repairs are retained separately from method-value claims. A future native experiment needs explicit reconciliation of the failed admission and unknown accounting, a bounded revised admission decision and a new freeze that retains both prior outcomes. Recheck actual bootstrap compatibility and read isolation before inference. A valid small completed turn with final accounting must still precede any comparison. The unused reserved product context must remain hidden from a future candidate author until that candidate is frozen.

## Verified candidate boundaries

Instrument source through `207afde` passes **293/293 tests**, zero failures/skips on Node 24.18.0/macOS. Lint, formatting, documentation links and unchanged generated `dist/` pass. Cognitive complexity maximum is 15, with zero functions over the project threshold; this remains a static heuristic. [Validation and raw logs](evidence/validation.json) bind the local observations.

The actual npm archive was extracted and passed the existing package smoke (three host layouts, subset/customization preservation, inspectors, application evidence and restart checks). Its new public fixture inventory and all 347 extracted suite files match their public hashes; **5/5** packaged adoption checks pass. The new packed integrity check is also part of the existing CI smoke. Frozen maintenance artifacts disable Git text conversion, including binary archive/bundle files. This protects bytes across checkouts; it is not Windows native-execution support.

The checkpoint/log documentation changes the archive bytes, so the final archive is recreated and smoke-tested after the delivery commit; its exact SHA-256/SHA-512 and fresh platform job results belong to the linked PR. Do not substitute an earlier archive digest or PR #34's CI. The private frozen campaign remains bound to its older executed driver `2ab754d`; later fixes intentionally invalidate its current-driver admission pin rather than mutating its history.

The public suite is a declared privacy-preserving derivative with a separate HEAD/freeze. It omits the reserved transfer. The [export guide](../../../evaluation/maintenance-fixtures/README.md) documents what can be reproduced, original versus public hashes, retained V1/V2 failures and the corrected temporary-path normalization. Do not mix it into the original native permit.

## Next action

Review the linked draft PR and its exact artifact and platform results. Use `gh pr view codex/maintenance-resumption-value --json url,headRefOid,statusCheckRollup` to recover current remote state. Correct only identified defects and repeat affected checks plus repository gates. Ordinary strong tests remain preferred for the previously evaluated uses; keep the lab experimental. Do not interpret this checkpoint, a local shell probe or green package CI as completion of the native research objective.
