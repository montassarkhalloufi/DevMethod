# Ordinary-agent capability probe — 2026-09-16

Question fixed before dispatch: does retiring a past product compromise while preserving later independent choices require a new DevMethod mechanism in this existing application?

This is one development task with an ordinary capable task agent, not a native matched A/B/C campaign, a blind evaluation, a human study or a cost measurement. Previous stopped campaigns stay closed. Existing Séance is a known development context; no transfer claim. Same current task model/default reasoning, ordinary files/Git/scripts/tests allowed. No candidate tool is supplied. Do not require unnecessary journal or framework. One author, isolated worktree, 12 minute wall cap including implementation and checks. Parent independently reviews concrete preservation and unresolved intent. Browser checks are engineering verification. No money/tokens inferred from time.

Common brief: add a usable way to reconsider the guest-call compromise after cancellation, preserve the current title, later independently selected duration/order/film omissions and all published snapshots. Restoring an old snapshot is not the requested outcome. If the original reason for an omission is unknown, present alternatives or ask; do not fabricate it. Keep existing design and local operation. Existing valid saved documents remain accepted. No automatic publication. User can inspect the result before applying, cancel, and reload. Show the actual changed schedule and any retained uncertainty. Record what the ordinary agent can do and the integration/setup work it requires.

Decision rule: if ordinary tools implement the operation with the explicit preservation requirements, reject the claim that a special decision runtime is necessary for this case. This does not establish that every app is equally easy or that a reusable mechanism has no value. A failure may justify a narrower candidate only after diagnosing its actual cause, never by weakening the ordinary prompt.
