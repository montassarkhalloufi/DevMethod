# Current architecture

Status: accepted. PostgreSQL is the production persistence choice. The memory adapter supports local examples only and cannot prove multi-instance safety. React and TypeScript are the existing UI stack. No production credentials or external deployment are part of this task.
