# Fictional volunteer event
A slot has capacity 1. Concurrent requests must never confirm more than one reservation. Previously confirmed reservations must remain intact. Keep the API shape. This is a review-only request; do not modify the implementation.
